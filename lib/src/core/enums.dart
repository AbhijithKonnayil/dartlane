enum Status { completed }

enum BuildType {
  debug,
  release,
  profile,
}

enum TargetPlatform {
  androidArm,
  androidArm64,
  androidX86,
  androidX64,
}

enum ExecutableType {
  aar,
  apk,
  appbundle,
  bundle,
  linux,
  web,
  ipa,
  ios,
  windows
}
